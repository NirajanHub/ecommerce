package com.landing.presentation.viewModel

import com.ncodes.ecommerce.landing.presentation.viewModel.LandingViewModel
import org.junit.Test

class LandingViewModelTest {
     private lateinit var viewModel : LandingViewModel

    fun setUp(){
       viewModel = LandingViewModel()
    }
    @Test
    fun `get all the products that is loaded first when the user sign's up`(){

    }
}